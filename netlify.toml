[build]
    publish = "dist"
    functions = "netlify/functions"
[functions]
  external_node_modules = ["express", "dotenv", "cors", "mongodb"]
  node_bundler = "esbuild"
[[redirects]]
  from = "/netlify/*"
  to = "/.netlify/functions/index"
  status = 200
  force = true